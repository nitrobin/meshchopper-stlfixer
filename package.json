{
  "name": "stlfixer-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo with 3D printing / misc CLI utilities",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.11"
  }
}
